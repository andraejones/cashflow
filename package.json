{
  "name": "cashflow-calendar",
  "private": true,
  "description": "Offline-first personal finance calendar. No build step; open index.html directly.",
  "scripts": {
    "test": "node scripts/verify-logic.js && node scripts/verify-walk-parity.js",
    "test:ui": "node scripts/verify-ui.js",
    "test:sync": "node scripts/verify-sync.js"
  }
}

# OS junk
.DS_Store
.DS_Store.gemini/
._*
Thumbs.db
ehthumbs.db
Desktop.ini

# Editor / IDE
.vscode/
.idea/
*.swp
*.swo
*~
*.sublime-project
*.sublime-workspace

# Logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# Environment / secrets
.env
.env.local
.env.*.local
.envrc

# Node (future-proofing — project is currently buildless)
node_modules/

# Local exports / backups / bank statements — personal financial data,
# must never reach GitHub (repo is public)
cashflow_data_*.json
cashflow_backup*.json
*.csv
*.bak
*.tmp

# Temp / scratch
temp/
tmp/

# Local development scratch — experiments, throwaway scripts, sample data.
# Never committed; nothing the app loads may live here.
dev/

# Bug-fix loop handoff

One file reviewed per session, in the fixed order below. Sessions share NO context except this file — keep it complete and concise.

## Checklist

- [x] js/debt-snowball.js — 2026-07-03, 4 fixed / 3 log-only
  - Note: this session found an uncommitted WIP diff in this file (a prior session evidently died before verify/commit). The WIP was reviewed line-by-line, validated (TEST 24 fails without it), regression-tested, and shipped as part of this session's commit.
  - FIXED (MEDIUM) `cleanupOrphanedDebtMinimums` (~2145): duplicate-occurrence dedupe was per-date, so a schedule change (e.g. business-day adjustment edit) relocating an occurrence's landing date left a stranded `modifiedInstance` copy at the old date plus a fresh expansion at the new date — same occurrence, two dates, double-counted forever. Fix: elect ONE keeper per `recurringId|occurrence` across ALL dates, preferring a non-modified copy (it sits at the schedule's current date; the adjust pass re-applies reductions there), else the earliest; all other copies tombstoned. TEST 24.
  - FIXED (MEDIUM) `adjustMinimumPaymentTransactions` (~2257): reconciled the whole month against the walk's target, but the walk only schedules/pays occurrences from projection start (today+1) — a multi-occurrence month (semi-monthly/weekly) straddling today had its already-made early payment zeroed against a future-only target. Fix: instances dated < projection start are excluded (historical facts). TEST 25a.
  - FIXED (MEDIUM) same method (~2331): the target was allocated from the END of the month backwards, keeping a minimum dated AFTER the payoff day and zeroing the pre-payoff one. Fix: allocate chronologically (walk pays in date order until payoff, suppresses the rest). TEST 25b.
  - FIXED (MEDIUM) `syncSnowballTransactionsForMonth` (~2406): with auto-generate OFF, the next render's horizon sweep (includeExtra=false) silently deleted the row "Generate for Current Month" had just created. Fix: button-created rows are stamped `snowballForced: true` and kept by the off-sweep; they reconcile normally (match/re-date/delete) whenever includeExtra is true. Product choice: the forced row is user intent and is kept even though the auto-off projection doesn't model it (minimums dated after it are NOT suppressed while auto-generate stays off — accepted, self-corrects once the row's date passes into the historical snapshot). TEST 26.
  - LOG-ONLY (LOW) ~2313: the un-hide branch clears `modifiedInstance` on id-bearing rows without tombstoning; the next re-expansion's clear pass (recurring-manager) drops them untracked → remote merge resurrects a hidden $0 copy → the keeper election tombstones it a render later. Self-healing ping-pong, zero balance impact (amount 0, hidden). Real fix belongs in recurring-manager (see Cross-file leads).
  - LOG-ONLY (LOW) `computeMinimumPaymentEndDate` alreadyPaid branch (~2016) + `cleanupOrphanedDebtMinimums`: a debt whose CLEARING payment happened earlier in the current month goes `alreadyPaid` → endDate snaps to prev-month end → cleanup deletes the real clearing payment (it's past endDate) → snapshot un-pays the debt → instance regenerates → ~2-render oscillation until the month rolls over. Same family as the documented "payoff creep + flicker" quirk (memory: snowball-projection-start-monthend), which was deliberately left — locking past payoffs vs re-planning is an unresolved product call. Do not "fix" casually.
  - LOG-ONLY (INFO): `renderPlan` has an unused `settings` local (~2690). Same-day ordering differs between the projection walk (infusions before minimums) and the historical snapshot (transactions before infusions) — payoff-day attribution can differ by cents in clamped edge cases; display-only.
- [x] js/transaction-ui.js — 2026-07-03, 0 fixed / 3 log-only (INFO). Full read of all 2392 lines; suspicious spots verified against transaction-store.js + recurring-manager.js. No MEDIUM+ bugs.
  - VERIFIED-CLEAN (allocation draw provenance): edit-in-place changing the draw target leaves stale `existing.drawsFromRecurringId`/`drawsFromPeriodDate` on the merged object, but store `_applyAllocationDraw` (transaction-store.js ~1116) re-stamps them for a recurring target or deletes them for a non-recurring/dangling one — no stale provenance survives. Move branches carry provenance only when target unchanged; a changed target is re-stamped by the re-add. Correct.
  - VERIFIED-CLEAN (recurring add double-count): `addTransaction` adds a manual `firstInstance` (with recurringId) AND the definition; `addRecurringTransactionToDate` (recurring-manager.js ~1358) dedupes by `recurringId`+occurrenceKey so expansion never duplicates it.
  - VERIFIED-CLEAN (stale positional index): `saveEdit` is synchronous (no await between id-resolve and mutate); `deleteTransaction`/`toggleSettled`/close-out all re-resolve the live index by `id`. One-time txns get an `id` in `store.addTransaction`, so the id-based protection is real.
  - VERIFIED-CLEAN (innerHTML/XSS): every user string rendered via `createTextNode`/`textContent`; only numeric `toFixed` interpolation in `renderModalBalance`; `<option>.textContent` used for allocation labels.
  - LOG-ONLY (INFO) ~2134: `addTransaction` balance-dup guard tests `t.type === "balance"` without the `hidden !== true` filter used by `hasBalanceTransaction` (~779). Functionally equivalent today (balance txns are never hidden — only debt payments are). No fix.
  - LOG-ONLY (INFO) ~36: `_boundEscapeHandler` is added to `document` in the constructor and never removed (no destroy path). Single long-lived instance, negligible.
  - LOG-ONLY (INFO) ~1443-1449 + ~1862: `doSettle`'s recurring branch carries `drawsFromAllocationId`/provenance, but recurring instances can't draw (draw UI is one-time-only) so that carry is dead-but-harmless; likewise `saveEdit`'s recurring-move omits `closeoutDate` but the one-time default resolves to newDate, identical to what carrying would produce. Both intentional/inert.
- [x] js/recurring-manager.js — 2026-07-03, 1 fixed / 2 log-only. Full read of all 1952 lines.
  - FIXED (LOW→sync-correctness) clear passes in `applyRecurringTransactions` (~321) and `_applyCachedTransactions` (~549): both filtered out `recurringId && !modifiedInstance` rows to drop pure expansions, but an id-bearing row whose `modifiedInstance` flag was cleared elsewhere (debt-snowball un-hide branch, debt-snowball.js ~2317, keeps the synced `id`) was dropped WITHOUT a tombstone → CloudSync._mergeById resurrected the remote copy → sync ping-pong. Fix: extracted shared `_clearRecurringExpansions(transactions, dateString)` that calls `store.trackDeletedTransaction(t.id)` before dropping any id-bearing non-modified recurring row; id-less pure expansions are dropped untombstoned as before. Verified safe: the only producer of this anomaly is the snowball un-hide (re-hide gets a fresh id via markModified, so the tombstoned id is never reused); snowball payoff rows have no recurringId; all id-bearing minimum rows carry modifiedInstance. Closes the cross-file lead. TEST 27.
  - LOG-ONLY (INFO) `applyDaySpecificMonthlyRecurrence` (~935): `maxOccurrences` is gated on `monthsSinceStart`, which over-counts months where the Nth-weekday rule yields no occurrence (e.g. "5th Monday"); the sibling `countOccurrencesBefore` iterates months and counts real occurrences, so the two can disagree at the tail of a capped day-specific series. Pre-existing, narrow edge; not touched.
  - LOG-ONLY (INFO) `editTransaction` future-scope (~1800): `if (recurringTransaction)` is always truthy (already dereferenced above); dead guard, harmless.
  - VERIFIED-CLEAN (business-day cross-month): expansion checks month offsets [-1,0,1] only when businessDayAdjustment set; adjacent-month occurrences filtered back to the viewing month by filterYear/filterMonth. Correct.
  - VERIFIED-CLEAN (custom-interval infinite loop): non-positive/NaN customInterval guarded (~1264); month-stepped catch-up clamps month-end via getCustomIntervalDate. Correct.
- [x] js/transaction-store.js — 2026-07-03, 1 fixed / 2 log-only. Full read of all 1916 lines. Cross-file contracts verified against cloud-sync.js.
  - FIXED (MEDIUM, data-recovery path) `resetData` (~561): a decrypt/parse failure sets `_loadFailed`, which makes `saveData` refuse to persist (correct — it protects the intact on-disk ciphertext). But `resetData` is the user's "wipe everything / recover" action; it replaces in-memory state with a known-good empty state exactly like `importData` (which clears `_loadFailed` at ~1895) — yet it left `_loadFailed` set. Result: after a corrupt/failed load, the user clicks Reset, sees "All data has been reset", but the wipe never persists and the corrupt data returns on reload. Fix: set `this._loadFailed = false` before `saveData()`, mirroring importData. TEST 28.
  - LOG-ONLY (LOW) `loadData` (~236-266): a legacy recurring transaction missing `_lastModified`/`id` is stamped in memory but does NOT set `_needsMigrationSave` (only the lastDayOfMonth migration and the transactions migration do). If ONLY recurring lack timestamps, the stamp isn't persisted until the next real save. Harmless — merge uses in-memory (fresh ts wins), and any transaction needing migration triggers a full save that persists recurring too.
  - LOG-ONLY (INFO) `_pruneDeletedItems` (~407-417): old-format tombstones (bare string ids, no `deletedAt`) are kept forever (`return true`) — comment says "will be replaced on next delete" but they never are unless the same id is re-deleted. Bounded by legacy delete count; negligible growth.
  - VERIFIED-CLEAN (date-change under same id): `rollForwardAllocations`/`moveTransaction` re-date a transaction while keeping its id + bumping `_lastModified`; cloud-sync `_mergeTransactions` (~620-647) has an explicit newestDateById dedup that collapses the same id appearing under two dates to the newest. Contract upheld.
  - VERIFIED-CLEAN (monthlyNotes deletion): `setMonthlyNotes("")` deletes the key with no tombstone, but cloud-sync has a dedicated `_mergeMonthlyNotes` (not a plain union) — deletion propagation is its responsibility, handled there.
  - VERIFIED-CLEAN (snowballForced passthrough): all persistence is JSON.stringify of whole objects; `_filterPersistedTransactions` selects WHICH transactions to keep, never strips fields. `snowballForced` (and any unknown field) survives save/load/export/import. Closes the cross-file lead below.
  - VERIFIED-CLEAN (allocation draw round-trip): `_applyAllocationDraw`/`_reverseAllocationDraw` net correctly across add/update/delete; `updateTransaction` refunds old draw then re-draws merged amount/target; overflow clamps to bucket remaining and stores exact `drawAmount` for reversal.
  - VERIFIED-CLEAN (local-date helpers): `_todayString`, `autoSettleExpiredRecurring`, `rollForwardAllocations` all build the date string from local `getFullYear/getMonth/getDate` — identical to `Utils.formatDateString`. No UTC off-by-one.
- [x] js/bank-reconcile.js — 2026-07-03, 1 fixed / 3 log-only. Full read of all 1610 lines. Cross-checked moveTransaction + allocation-draw reversal against transaction-store.js.
  - FIXED (LOW, misclassification) `_parseSuncoastCsv` (~209): `pending` was set purely from Balance $0.00, so a real posted WITHDRAWAL that empties the account (Balance 0.00 with a Withdrawal amount) was misread as a pending hold → suppressed its Settle / date-drift actions (both gated on `!b.pending`) and gave it the wider 7-day unsettled match window. Per the documented Suncoast format a pending hold always carries its amount in the Deposit column (never Withdrawal), so gated `pending` on `withdrawal === null`. Sign unchanged (the deposit-sign branch only runs when withdrawal===null, where the new condition is identical). TEST 29.
  - LOG-ONLY (LOW) `_currentIndex` (~1519): the no-id field fallback (type+amount+description+recurringId) searches the LIVE store list, which includes `hidden` rows, while appItems exclude hidden. A pure recurring expansion drift (no id) could in theory resolve to a hidden same-key row. Requires two same-recurringId/amount/description rows (one hidden) on one date — not producible today (one occurrence per date; hidden rows are debt payments with the debt-name description). No fix.
  - LOG-ONLY (INFO) `_toIsoDate` (~299): accepts day 1–31 for any month, so an impossible bank date (e.g. 2/31) would pass and `parseDateString` would roll it forward. Bank CSVs never emit invalid dates; cosmetic robustness only.
  - LOG-ONLY (INFO) `_parseSuncoastCsv` returns a `window` field (~234) that no caller reads — `_run` recomputes via `_statementWindow`. Dead but harmless.
  - VERIFIED-CLEAN (recurring relocate double-count): `_relocateEntry` recurring branch does skip + `store.moveTransaction` + one-time copy. `moveTransaction` only writes the `movedTransactions` display-bookkeeping map — recurring-manager NEVER reads it during expansion — so it does not place a second instance at the target. The skip suppresses the original; only the one-time copy lands at target. No double-count.
  - VERIFIED-CLEAN (allocation-draw double-debit): `_relocateEntry`/`_fixDate`/`_settle` delete the original then re-add a copy. `store.deleteTransaction` reverses the draw (store.js:1248) and `store.addTransaction` re-applies it (store.js:1182) — symmetric, net-zero on the bucket.
  - VERIFIED-CLEAN (match double-report): pass 2/3 set `matched` but not `_match`/`_matchedBank`; every downstream section keyed on `_match` (clearedUnsettled/dateDrifted/pendingMatched) or `_matchedBank` (appPending*) therefore excludes review-pair items, which appear only in reviewPairs. clearedUnsettled vs dateDrifted are mutually exclusive by the `settled===false` guard. No item is reported twice.
  - VERIFIED-CLEAN (XSS): all user strings rendered through `_esc`/`_attr`; only ISO dates and numeric `data-i`/`toFixed` interpolated raw into innerHTML.
- [x] js/cloud-sync.js — 2026-07-03, 1 fixed / 2 log-only. Full read of all 1521 lines. Merge paths cross-checked against transaction-store.js exportData/importData + persisted-id guarantees.
  - FIXED (MEDIUM, data-loss) `saveToCloud` (~1107): the fetch-and-merge-before-push step was gated on `if (this._lastKnownETag)`. When ETag is null (device has NEVER synced this gist — e.g. a fresh device that entered credentials during the save prompt and never ran Load, or ETag cleared) the merge was skipped and the PATCH blind-overwrote a populated remote gist, destroying another device's data. Push is the documented merge-and-protect path (see `_hasLocalChangesSinceSync` comment: merge is Save's job, not Load's), so gating it was a hole. Fix: always fetch (If-None-Match only when a stored ETag exists); a null ETag just means the GET returns 200 with remote data to merge. Empty/other gist (no `cashflow_data.json`) still falls through to a clean push. TEST 30.
  - LOG-ONLY (LOW, deletion-resurrection) `_mergeMonthlyNotes` (~717): a note the user CLEARED (`setMonthlyNotes("")` removes the key, no tombstone) is indistinguishable from "never had it" — on merge the remote's stale text resurrects (`!localData.text` branch → `merged = remoteNote`). NOTE: transaction-store.js session marked this "verified-clean / handled in _mergeMonthlyNotes" but it is NOT actually handled — clearing does not propagate. Real fix needs a monthly-note tombstone in the store (cross-file); left log-only (notes are low-stakes, rarely deleted, and a re-clear on any device re-wins by content). See Cross-file leads.
  - LOG-ONLY (INFO) `scheduleCloudSave` (~436): `_lastSaveTime = Date.now()` is stamped at SCHEDULE time, so the 90s heartbeat grace suppresses genuine remote-change detection for 90s even if the queued save is later dropped (no credentials) or fails. Intentional per the inline comment; negligible UX only.
  - VERIFIED-CLEAN (id-less merge drop): `_mergeById` drops items without an `id`; safe because every PERSISTED transaction has one (one-time via addTransaction ~1174; load migration stamps legacy ~213; modifiedInstance rows get ids) and `exportData` only ships `_filterPersistedTransactions` output (pure expansions excluded). No persisted row is id-less.
  - VERIFIED-CLEAN (heartbeat generation guard): `_heartbeatGen` snapshot-after-stop / bail-after-await is correct; `_heartbeatChecking` prevents overlapping ticks; `checkForRemoteChanges` intentionally lock-free (memory: cloud-sync-concurrency). Matches documented model.
  - VERIFIED-CLEAN (transaction re-date dedup): `_mergeTransactions` newestDateById collapses a same-id row appearing under two dates (rolling allocations / moveTransaction) to the newest `_lastModified`. Correct; upheld by transaction-store contract.
  - VERIFIED-CLEAN (skip LWW merge): `_mergeSkippedTransactions` + `_deletedItems.skips` event LWW propagate unskips over stale union skips (TEST 22). Correct.
- [x] scripts/verify-logic.js — 2026-07-04, 0 fixed / 4 log-only (INFO). Full read of all 2077 lines (the checklist's 1173 is stale — the file grew as sessions 2–6 appended TESTs 20–30). Verified: suite passes (`node scripts/verify-logic.js`, exit 0); gate is sound (sync throws → uncaught → exit 1; async TEST 30 failure → `.catch` → `process.exit(1)`, empirically confirmed exit 1); mock `Utils` helpers match production semantics for every tested path; the `Utils.*` methods the mock omits (MONTH_LABELS, DAY_SPECIFIC_OPTIONS, buildXOptions, showModal*, show/hideLoading) are only reached by constructor/display paths that tests bypass via `Object.create(prototype)` + manual field-set or self-patch (TEST 30). No false-pass tests found.
  - LOG-ONLY (INFO) TEST 30 (~2057): the terminal test is async and prints "ALL TESTS PASSED" from inside its `.then`. Two consequences: (1) any test appended AFTER it would run before the banner and break ordering — new tests must go before TEST 30; (2) there is no independent "all tests ran" guard, so a hypothetical non-settling `saveToCloud` would let node exit 0 with no banner (silent pass). saveToCloud always settles here, so not a live bug. No change.
  - LOG-ONLY (INFO) mock `Utils.parseDateString` (~24): diverges from production — throws on null/undefined (`str.split`) and yields NaN on ISO "…T…" input, whereas real Utils returns null and strips the "T" part. No current test passes such input; a future test that does would observe harness-only behavior. Left as-is (matching prod could also mask a real null-passing bug — a deliberate tradeoff).
  - LOG-ONLY (INFO) mock omits `Utils.MONTH_LABELS`/`DAY_SPECIFIC_OPTIONS`/`buildXOptions`/`showModalAlert`/`showModalConfirm` used by debt-snowball/recurring display+form paths. Any future test that drives `formatPayoffDate`/`formatMonthYear` (debt-snowball ~987–1008) or a real `DebtSnowballUI` constructor would throw on `undefined[...]`. Untested surface only today.
  - LOG-ONLY (INFO) real-date tests (1,2,3,5,7) read system `Date` unfrozen; TEST 3 (~104) uses `setMonth(getMonth()+1)` which overflows on 31st/short-month boundaries (e.g. Jan 31 → Mar 3). Suite still passes because the assertions are existence checks, but it's latent date fragility. No change.
- [ ] js/calendar-ui.js (959)
- [ ] js/pin-protection.js (870)
- [ ] js/app.js (776)
- [ ] js/utils.js (756)
- [ ] js/calculation-service.js (597)
- [ ] js/search-ui.js (544)
- [ ] js/build.js (4)

## Cross-file leads

- **[RESOLVED 2026-07-03]** recurring-manager clear-pass tombstone lead — fixed via `_clearRecurringExpansions` (see recurring-manager.js checklist entry, TEST 27). The debt-snowball un-hide branch (debt-snowball.js ~2317) still clears `modifiedInstance` without tombstoning at the source, but the clear pass now catches its dropped id-bearing rows, so the ping-pong is closed. No further action needed unless a new anomaly producer appears.
- **[RESOLVED 2026-07-03]** `snowballForced` field survival — verified in transaction-store.js: JSON.stringify passthrough of whole objects, `_filterPersistedTransactions` only selects rows (never strips fields). Survives save/load/export/import. cloud-sync `_mergeById` is object-preserving too. No stripping path exists.
- **[cloud-sync → transaction-store]** Monthly-note deletion does NOT sync: `setMonthlyNotes("")` removes the key with no tombstone, and `_mergeMonthlyNotes` resurrects the remote copy (cloud-sync.js ~717 log-only above). A real fix needs a monthly-note tombstone (e.g. an entry in `_deletedItems` or a cleared-marker object with `_lastModified`) written by the store on clear, then honored in `_mergeMonthlyNotes`. The transaction-store.js checklist entry's "verified-clean" claim for this is INCORRECT — reopen if monthly-note loss is reported. Low priority (notes rarely deleted).
- Data contract relied on this session: occurrence identity for recurring instances is `recurringId|originalDate||placedDate` (recurring-manager keys modified instances the same way); `store.trackDeletedTransaction(id)` is a safe no-op for undefined ids.

## Verified not bugs

- `store.moveTransaction(recId, from, to)` is DISPLAY-ONLY bookkeeping: it writes the `movedTransactions` map, read only by `hasMoveAnomaly`/`getMoveForRecurring` for calendar move markers. recurring-manager expansion does NOT consult it, so it never relocates an occurrence — the skip+move+one-time-copy relocate pattern (bank-reconcile `_relocateEntry`, carried-forward Settle) is not a double-count.

- debt-snowball keeper election can prefer a future non-modified copy over a PAST modified copy of the same occurrence, effectively re-dating a real payment — intentional: it only happens after the user edits the schedule, and the adjust pass re-applies reductions at the current date.
- `calculateInfusionAllocations` projection start-month math — already fixed previously (absolute month-index min), guarded by TEST 10.
- Eager ~600-month `projDays` array (~18k entries) in `calculateSnowballProjection` when debts never clear — bounded, by design (the "Not yet on track" case), acceptable perf.
- `while (true)` payoff sweep in the daily walk terminates: each iteration either breaks or zeroes a debt, strictly shrinking the active set.
- `ensureSnowballPaymentForMonth`'s save condition (`changed && !snowballAdded && !syncResult.changed`) looks odd but is right: sync saves for itself, addTransaction debounce-saves, this covers prune/adjust-only changes.
